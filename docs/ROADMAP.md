

## Latest continue (按你推荐的继续 2026-06-07)

**Fedora '带界面的版本' GUI test version verified deliverable:**
- examples/run-fedora-gui-test.sh executed (headless xvfb + VNPY_DEMO_DATA_ONLY): printed full customer acceptance checklist (1-11: pro DOM/Tape/Watchlist/Chart with POC/HVN + footprint/imbal per WenHua/KuaiQi, fusion, bilingual, zero-mutex, REAL CEF path, py parity).
- Launcher py path: [SampleStrategy] REAL on_tick via engine/Ports (dozens, 5200+ varying prices sustained); [PyStrategy] buy/sell submitted multiple via backtest ports wiring; [BacktestLauncher] REAL run complete trades=2 pnl=1.00 commission=0.10 (engine.get_result()); "Python strategy execution path used (on_tick from user .py code fired via backtest engine dispatch + host handler)".
- CEF: reconfig with -DVNPY_TERMINAL_USE_CEF=ON -DCEF_ROOT=/tmp/cef_dummy (cache ON, linked libcef; dummy for TDD/compile parity, real tar manual per cef-setup.md for full visuals).
- Skills/MCP: ~/.claude/skills/ 334+; .skillshare project 30+ top (playwright-pro full 9 subs + agents, mcp-builder, tdd-workflow, check-work, code-reviewer, cpp/python-*, agent-*/ai-*/research, frontend, e2e, security etc. + custom vnpy-backtest-launcher-demo); skillshare list/sync exercised. MCP: search_tool first for schemas (chrome-devtools__list_pages etc + grok_com_github__push_files exact: owner/repo/branch/files[]/message); used for list_pages (browser surface) + this push.
- Fix: added footprint_panel.hpp include to main.cpp for clean build with pro order flow panel (TDD for Phase 6+).
- Deliverable: build/app/vnpy-terminal + build/vnpy-fedora-gui-test-*.tar.gz + run-fedora-gui-test.sh + docs/fedora-gui-customer-test.md (checklist + handoff + sign-off section) + cef-setup + sample_strategy.py. One-command for customer on Fedora: ./run-fedora-gui-test.sh (real display interactive or xvfb evidence logs). Matches SRS '带界面的版本' immediate acceptance target + AGENTS living docs (srs.md 《产品需求规格说明书》, SYSTEM_DESIGN_BASELINE 《系统详细设计说明书》).

Per user '按你推荐的继续' + original queries (ECC/Grok Build best practices, strict agile/TDD, docs PRD/SRS+SDS, auto download/install skills/MCP/plugin any time via skillshare, '带界面的版本' for customer 验收/试用). GUI test version **immediately deliverable now**. Next: provide real CEF tar URL or CTP account for full production; confirm sign-off.

Evidence tools: run fedora script + DATA_ONLY captures, ls skills/tars, cmake reconfig, grep on_tick/trades/path, search_tool MCP schemas, use_tool list+push, todo_write, reads of AGENTS/ROADMAP/fedora/cef/SKILL.md.

(Continue per AGENTS: one in_progress todo, verification gates, conventional, living baselines.)

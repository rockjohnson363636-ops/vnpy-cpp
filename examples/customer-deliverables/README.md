Customer deliverable artifacts for '带界面的版本' Fedora GUI test/验收.

- vnpy-cpp-fedora-gui-test.tar.gz (1.6M): the full test package (binary + run script + py strat + docs).
Extract and run ./run-fedora-gui-test.sh per ROADMAP / DELIVERY notes.

Updated in '按你推荐的继续' re-verify continuation (CEF real tar instructions, fresh py evidence).